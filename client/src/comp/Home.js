import React from 'react';
import logo from '../logo.png';

const Home = (props) => {
    return (
        <div className="home-page">
            <img src={logo}/>
        </div>
    );
};

export default Home;
